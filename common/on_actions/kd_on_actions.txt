on_actions = {
    on_daily = {
        effect = {
            every_country = {
                limit = {
                    tag = GER
                    has_war = yes
                    NOT = { controls_state = 64 }
                    NOT = { has_country_flag = kd_doenitz_taken_over }
                }
                country_event = { id = kd_doenitz.1 days = 0 }
            }
        }
    }
}
